export const PUBLIC_LIBRARY = true;
export const CLOUD_LIBRARY = false;
// Kept for the older server build, which overwrites this file. The published app has no server:
// its collection is whichever folder the reader chooses on their own device.
export const CLOUD_HOME = '';
